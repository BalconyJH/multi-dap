{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Attach to multi-dap",
      "type": "multi-dap",
      "request": "attach",
      "configFile": "${workspaceFolder}/project.toml"
    }
  ]
}
